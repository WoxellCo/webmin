#ifndef WEBMIN_JS_MIN_H
#define WEBMIN_JS_MIN_H
#if 0
#include <stddef.h>
#include <stdint.h>

#include "webmin_general.h"

webmin_context_t webmin_js(char *s, char *delims[], size_t max_length, uint8_t flags, webmin_context_t *concat);
#endif
#endif